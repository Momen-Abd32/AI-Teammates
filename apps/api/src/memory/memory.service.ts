import{Injectable}from"@nestjs/common";import{randomUUID}from"crypto";import{Memory,MemoryScope}from"../domain";import{MemoryRepository}from"../repositories/memory.repository";@Injectable()export class MemoryService{constructor(private repo:MemoryRepository){}create(input:Omit<Memory,"id">){if(!input.content.trim())throw new Error("Memory content is required");return this.repo.save({...input,id:randomUUID()})}async list(agentId:string,scope?:MemoryScope){const rows=await this.repo.findByAgent(agentId);return rows.filter(x=>!scope||x.scope===scope)}delete(agentId:string,id:string){return this.repo.delete(agentId,id)}}