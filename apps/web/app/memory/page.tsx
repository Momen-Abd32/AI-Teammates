import {apiFetch} from "../../lib/auth";
"use client";import{useEffect,useState}from"react";const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:3001/api";export default function Memory(){const[r,setR]=useState<any[]>([]);useEffect(()=>{apiFetch("/memory/agent-demo").then(x=>x.json()).then(setR).catch(()=>{});},[]);return <main style={{maxWidth:900,margin:"40px auto",fontFamily:"Arial",padding:24}}><h1>Work Memory</h1><p>Professional context only.</p>{r.map(x=><div key={x.id} style={{padding:14,border:"1px solid #ddd",margin:"8px 0"}}><small>{x.scope}</small><p>{x.content}</p></div>)}</main>}